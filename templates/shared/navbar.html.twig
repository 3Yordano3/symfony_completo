<div style="
    z-index: 100;
    position: fixed;
    top: 0px;
    width: 100%;">
    <header aria-label="Site Header" class="border-b border-gray-100 navbar-site-transparent" id="navbar-site-transparent">
        <div
                class="mx-auto flex h-16 items-center justify-between"
        >
            {% set route_name = app.request.attributes.get('_route') %}
            <a style="padding-left: 20px" href="{{ path('app_home') }}" class="flex">
                Logo
            </a>


            <div class="flex flex-1 items-center justify-end">
                <nav
                        aria-label="Site Nav"
                        class="hidden lg:flex lg:gap-4 lg:text-xs lg:font-bold lg:uppercase lg:tracking-wide lg:text-gray-500"
                >
                    {% if route_name == "app_portfolio" %}
                        <a
                                href="{{ path('app_portfolio') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700 link-actived"
                        >
                            PortFolio
                        </a>
                    {% else %}
                        <a
                                href="{{ path('app_portfolio') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700"
                        >
                            PortFolio
                        </a>
                    {% endif %}

                    {% if route_name == "app_about" %}
                        <a
                                href="{{ path('app_about') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700 link-actived"
                        >
                            Qui suis-je?
                        </a>
                    {% else %}
                        <a
                                href="{{ path('app_about') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700"
                        >
                            Qui suis-je?
                        </a>
                    {% endif %}

                    {% if route_name == "app_pricing" %}
                        <a
                                href="{{ path('app_pricing') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700 link-actived"
                        >
                            Pricing
                        </a>
                    {% else %}
                        <a
                                href="{{ path('app_pricing') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700"
                        >
                            Pricing
                        </a>
                    {% endif %}

                    {% if route_name == "app_contact" %}
                        <a
                                href="{{ path('app_contact') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700 link-actived"
                        >
                            Contact
                        </a>
                    {% else %}
                        <a
                                href="{{ path('app_contact') }}"
                                class="block h-16 border-b-4 border-transparent leading-[4rem] hover:border-current hover:text-red-700"
                        >
                            Contact
                        </a>
                    {% endif %}
                </nav>

                <div class="ml-8 flex items-center">
                    <div
                            class="flex items-center divide-x divide-gray-100 border-x border-gray-100"
                    >
          <span>
            <a
                    href="/cart"
                    class="block border-b-4 border-transparent p-6 hover:border-red-700"
            >
              <svg
                      class="h-4 w-4"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      xmlns="http://www.w3.org/2000/svg"
              >
                <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"
                />
              </svg>

              <span class="sr-only">Cart</span>
            </a>
          </span>

                        <span>
            <a
                    href="/account"
                    class="block border-b-4 border-transparent p-6 hover:border-red-700"
            >
              <svg
                      class="h-4 w-4"
                      xmlns="http://www.w3.org/2000/svg"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
              >
                <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                />
              </svg>

              <span class="sr-only"> Account </span>
            </a>
          </span>

                        <span class="hidden sm:block">
            <a
                    href="/search"
                    class="block border-b-4 border-transparent p-6 hover:border-red-700"
            >
              <svg
                      class="h-4 w-4"
                      xmlns="http://www.w3.org/2000/svg"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
              >
                <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
                />
              </svg>

              <span class="sr-only"> Search </span>
            </a>
          </span>
                    </div>
                </div>
            </div>
        </div>
    </header>
</div>

<script>
    const myNav = document.getElementById('navbar-site-transparent');
    if(window.onscroll){
        console.log(window.scrollY);
    }
    window.onscroll = function () {
        if (document.body.scrollTop >= 60 || document.documentElement.scrollTop >= 60 )
        {
            console.log("bas");
            myNav.classList.add("nav-colored");
            myNav.classList.remove("nav-transparent");
        }
        else {
            console.log("top");
            myNav.classList.add("nav-transparent");
            myNav.classList.remove("nav-colored");
        }
    };
</script>